const loginAdmin = (req, res) => {
  // Get email
  // Get password
  // Get user
  // redirect if bad credentials
  // send token if valid credentials
  res.json({ login: true })
}

export default loginAdmin
